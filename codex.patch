 (cd "$(git rev-parse --show-toplevel)" && git apply --3way <<'EOF' 
diff --git a/run.py b/run.py
index 9e8f1a0930645b7598f939df3f1e25fc470e8d6f..478e761d2b5f8f0d7ce65a621334dc3bc6e2f94a 100644
--- a/run.py
+++ b/run.py
@@ -1,13 +1,32 @@
 from __future__ import annotations
 
 import sys
 
 
-if len(sys.argv) > 1 and sys.argv[1] == "gui":
-    from app.gui.app import main
-else:
+CLI_COMMANDS = {
+    "menu",
+    "login",
+    "dialogs",
+    "schedule",
+    "list-scheduled",
+    "cancel",
+    "preview-import",
+}
+
+
+def _is_cli_mode(argv: list[str]) -> bool:
+    if len(argv) <= 1:
+        return False
+    return argv[1] in CLI_COMMANDS
+
+
+if _is_cli_mode(sys.argv):
     from app.main import main
+else:
+    if len(sys.argv) > 1 and sys.argv[1] == "gui":
+        sys.argv.pop(1)
+    from app.gui.app import main
 
 
 if __name__ == "__main__":
     main()
 
EOF
)